 export const breadCrumbItemsSelector = ({ app }) => app.breadCrumbItems
export const toastSelector = ({ app }) => app.toast
export const localeSelector = ({ app }) => app.locale
export const regionSelector = ({ app }) => app.region